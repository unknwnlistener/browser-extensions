var config = {}

config.version= 1,
config.actions= {
    url: true,
    tab_opened: true,
    tab_closed: false,
    mouse_click: false,
    keystrokes: false
}

// // Call the config from the server
// $(document).ready(function() {
//     $.ajax(
//             { url:  }
//         )
// });